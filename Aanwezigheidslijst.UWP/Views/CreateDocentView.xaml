<Page
    x:Class="Aanwezigheidslijst.UWP.Views.CreateDocentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Aanwezigheidslijst.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns: viewModels ="using:Aanwezigheidslijst.UWP.vieModels"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
            
        <TextBox HorizontalAlignment="Left" Margin="80,88,0,0" PlaceholderText="Naam" VerticalAlignment="Top" Width="605" Text="{x:Bind ViewModel.Naam, Mode=TwoWay}"/>
        <TextBox HorizontalAlignment="Left" Margin="80,148,0,0" PlaceholderText="Bedrijf"  VerticalAlignment="Top" Width="605" Text="{x:Bind ViewModel.Bedrijf, Mode=TwoWay}"/>
        <Button Content="Save" Margin="80,206,0,0" VerticalAlignment="Top" Command="{x:Bind ViewModel.SaveCommand}" />

    </Grid>
</Page>
